---
title: "AdvDex: Learning Dexterous Manipulation from Human Demonstrations via Joint-Aligned Actions and Adversarial Learning"
title_zh: AdvDex：通过关节对齐动作与对抗式学习从人类示范中学习灵巧操作
authors: "Zhiyue Zhao, Jingyi Wu, Hairuo Liu, Mingyu Liu, Liyang Li, Hengdi Zhang, Tong He, Zhengxue Cheng"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14028v1"
tags: ["query:dexop"]
score: 10.0
evidence: 从人类演示中学习灵巧操作，并引入了一个大规模多模态数据集。
tldr: AdvDex提出统一视觉-语言-动作框架，利用含运动学与触觉的OmniShare人类演示、关节对齐的JAAS动作表征（腕位姿SE(3)+15指关节），并以域对抗削弱载体特征，缓解示教昂贵与跨载体差异。实验证明在动作预测与真实灵巧操控上超越基线，实现零样本人到机迁移、对新物体/环境泛化与高效少样本适配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14028v1/fig-001.webp\", \"caption\": \"Figure 1: AdvDex. A unified Vision-Language-Action framework for scalable cross-embodiment dexterous manipulation. By aligning hardware kinematics through a shared action space and reducing embodiment-specific visual information through domain-adversarial learning, AdvDex supports zero-shot generalization to novel objects and unseen environments.\", \"page\": 2, \"index\": 1, \"width\": 822, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2608.14028v1/fig-002.webp\", \"caption\": \"Table 3: Success rates on real-world robot dexterous manipulation tasks (in %).\", \"page\": 7, \"index\": 2, \"width\": 818, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2608.14028v1/fig-003.webp\", \"caption\": \"Figure 4: Overview of the Domain-Adversarial VLA. A domain discriminator connected to the Vision-Language Model through a Gradient Reversal Layer encourages the extracted cognition tokens to become more domain-invariant before they condition the Diffusion Transformer for joint-aligned action prediction.\", \"page\": 5, \"index\": 3, \"width\": 814, \"height\": 280}]"
motivation: 机器人灵巧操控难以扩展，因机器人示教成本高且不同载体动作空间与外观差异使策略难以跨载体泛化。
method: 在统一视觉-语言-动作框架下，构建OmniShare多模态人类演示（运动学+触觉）、提出对齐人机的JAAS动作空间（腕位姿SE(3)+15指关节），并用域对抗学习去除载体特征以学得通用视觉表征。
result: 在手部动作预测与真实机器人灵巧操控中稳健优于基线，能零样本人到机技能迁移、泛化到未见物体与环境，并以极少数据快速适配。
conclusion: 以JAAS对齐动作和域对抗表征结合大规模人类演示，可显著提升灵巧操控的跨载体泛化与数据效率，推动从人到机器人的技能迁移。
---

## 摘要
灵巧操作是具身智能的一项基础能力，但其规模化仍然困难，因为机器人示范的收集成本高昂，且不同具身形式的动作空间各不相同。在异构数据上训练的策略还可能将与任务相关的视觉线索与具身形式特有的外观纠缠在一起，从而限制跨具身的泛化。我们提出 AdvDex，这是一种统一的视觉-语言-动作框架，用于从人类与机器人示范中学习灵巧操作。首先，我们介绍 OmniShare，这是一个大规模多模态的人类操作示范数据集，提供高质量的运动学监督和触觉测量，同时减少对机器人遥操作的依赖。其次，我们提出关节对齐动作空间（JAAS），这是一种规范化的动作表示，由一个 SE(3) 手腕位姿和 15 个手指关节组成，从而在功能上对齐人手、灵巧机器人手和平行夹爪。最后，我们采用域对抗学习，减少学习到的视觉表征中的具身特定信息。在手部动作预测和真实世界灵巧操作上的实验证明了相对于基线的一致改进，实现了有效的零样本人到机器人的技能迁移、对未见物体与环境的泛化，以及数据高效的少样本适应。

## Abstract
Dexterous manipulation is a fundamental capability for embodied intelligence, but scaling it remains difficult because robot demonstrations are expensive to collect and action spaces vary across embodiments. Policies trained on heterogeneous data can also entangle task-relevant visual cues with embodiment-specific appearance, limiting cross-embodiment generalization. We present AdvDex, a unified Vision-Language-Action framework for learning dexterous manipulation from human and robot demonstrations. First, we introduce OmniShare, a large-scale multimodal dataset of human manipulation demonstrations that provides high-quality kinematic supervision and tactile measurements while reducing reliance on robot teleoperation. Second, we propose the Joint-Aligned Action Space (JAAS), a canonical action representation comprising an $\mathrm{SE}(3)$ wrist pose and 15 finger joints, thereby functionally aligning human hands, dexterous robot hands, and parallel grippers. Finally, we use domain-adversarial learning to reduce embodiment-specific information in the learned visual representation. Experiments on hand-action prediction and real-world dexterous manipulation show consistent improvements over baselines, effective zero-shot human-to-robot skill transfer, generalization to unseen objects and environments, and data-efficient few-shot adaptation.

---

## 论文详细总结（自动生成）

# 1. 核心问题与整体含义（研究动机与背景）
- 研究动机
  - 灵巧操作是具身智能的关键能力，但机器人示范收集昂贵、风险大、难以规模化。
  - 不同具身（人手、灵巧机械手、平行夹爪）在动作空间与外观上差异巨大，导致从异构数据学习的策略容易将“与任务相关的视觉线索”与“具身特有外观”混淆，限制跨载体泛化。
- 核心问题
  - 如何在统一的视觉-语言-动作（VLA）框架下，利用大量人类示范替代昂贵的机器人遥操作示范，并能跨具身泛化到多种手部硬件与新环境？
- 整体含义
  - 论文提出 AdvDex：通过规范化的动作表示对齐不同硬件的运动学（JAAS），并用域对抗学习去除具身特有视觉信息，从而实现从人类与机器人混合示范中学习、零样本人到机迁移、对新物体与环境的泛化，以及数据高效的少样本适配。

# 2. 方法论：核心思想、关键技术与流程
- 核心思想
  - 统一的 VLA 框架：以视觉与语言作为条件，预测跨具身共享的、功能对齐的动作表示（JAAS），并通过域对抗约束学得更“具身不变”的视觉表征。
- 关键组成
  1) OmniShare 数据集（人类示范）
     - 大规模多模态人类操作示范，包含高质量的手部运动学监督与触觉测量。
     - 目的：减少机器人遥操作依赖，用成本更低的人类示范提升数据规模与多样性。
  2) 关节对齐动作空间 JAAS
     - 定义：由一个 SE(3) 手腕位姿（平移+旋转）与 15 个手指关节变量组成的规范化动作表示。
     - 作用：在功能层面对齐人手、灵巧机械手和平行夹爪（将不同硬件的控制信号映射到同一“动作语义”空间），降低跨具身差异。
  3) 域对抗 VLA（Domain-Adversarial VLA）
     - 架构要点（参见文中 Figure 4 描述）：
       - 使用视觉-语言模型（VLM）从图像与指令中抽取“认知 tokens”。
       - 通过 Gradient Reversal Layer（GRL）连接一个“域判别器”，域标签可理解为不同具身/传感域（如人/机、相机/场景差异）。训练时最大化域判别器的损失以迫使特征“去具身化”，得到更域不变的视觉语义表示。
       - 将域不变的“认知 tokens”作为条件，输入扩散式 Transformer（Diffusion Transformer），预测 JAAS 动作序列。
     - 训练目标（文字描述）
       - 动作预测目标：以扩散模型的噪声对比/似然目标拟合 JAAS 动作分布（或等价的重建/去噪损失）。
       - 域对抗目标：最小化策略端的动作预测损失，同时最大化域判别器的判别损失（通过 GRL 实现），以削弱具身特定信息。
- 训练/推理流程（简述）
  - 输入：当前（或历史）视觉帧 + 语言指令（可选）+（训练时）对应示范的 JAAS 标注。
  - VLM 编码 → GRL 与域判别器对抗约束 → 域不变认知 tokens → 扩散 Transformer 条件生成 → 输出 JAAS（SE(3) 手腕位姿 + 15 指关节）。
  - 在不同具身上，通过各自的“JAAS↔硬件控制”映射执行。

# 3. 实验设计：数据、场景、基准与对比
- 数据与场景
  - OmniShare 人类示范数据：多模态（运动学+触觉），用于大规模监督与预训练。
  - 机器人与人类混合示范：用于跨域/跨具身训练与评估。
  - 两类评估设置（论文摘要与图注信息披露）：
    - 手部动作预测任务：评估从视觉-语言条件下预测 JAAS 的精度/误差。
    - 真实世界灵巧操控任务：在实际机器人平台上执行，报告任务成功率（Table 3：给出不同任务的成功率百分比；具体数值未在提供文本中列出）。
  - 泛化与迁移设置：
    - 零样本人到机迁移：直接将基于人类示范学到的策略部署在机器人上。
    - 未见物体与环境泛化：在训练未出现的新物体/场景中评测。
    - 少样本适配：用极少量新域数据进行快速微调并测试提升幅度。
- 基准与对比
  - 与多种“有代表性的基线”比较（原文未给出具体方法名与细节），覆盖：
    - 无 JAAS 的动作表征或硬件特定控制空间的策略；
    - 无域对抗学习的 VLA（直接用视觉-语言特征训练）；
    - 其他通用扩散式策略或常见机器人学习基线。
  - 消融实验（从图文信息可推断）
    - 去掉 JAAS；
    - 去掉域对抗模块（或移除 GRL/域判别器）；
    - 仅用人类/仅用机器人示范 vs. 混合训练；
    - 评估对零样本/少样本与 OOD 泛化的影响。
- 评价指标
  - 手部动作预测：JAAS 误差或姿态/关节角度误差（原文未披露具体指标公式）。
  - 真实任务：成功率（百分比，见 Table 3 标题）。

# 4. 资源与算力
- 论文提供内容中未明确说明训练/推理所用 GPU/CPU 型号、数量、训练时长等算力细节。

# 5. 实验数量与充分性
- 多维度设置：包含手部动作预测与真实机器人操控两类核心评估，并覆盖零样本人到机、未见物体/环境泛化、少样本适配等关键场景。
- 对比与消融：包含与多种基线的对比和关键组件（JAAS、域对抗）的消融，显示方法各组成的贡献。
- 充分性与客观性评价：
  - 优点：覆盖了跨具身泛化的主要挑战维度，既有人类到机器人迁移，也有真实世界任务成功率报告。
  - 局限：提供文本未包含任务数量、难度分布、各类物体/环境规模、统计显著性检验等细节；是否跨更多机器人平台与更复杂器具仍待确认。

# 6. 主要结论与发现
- 通过 JAAS 对齐动作空间与域对抗的 VLA 表征，模型在手部动作预测与真实机器人灵巧操控上均优于基线。
- 在零样本人到机迁移、未见物体/环境泛化、少样本适配等关键衡量上取得显著提升。
- 使用 OmniShare 等人类示范能有效替代大规模机器人遥操作数据，降低示教成本，同时保持或提升跨域泛化能力。

# 7. 优点（亮点）
- 统一的跨具身动作表示：JAAS 用 SE(3) 手腕位姿 + 15 指关节对齐不同硬件的功能操作语义，显著减小跨载体鸿沟。
- 域对抗学习的表征稳健性：通过 GRL+域判别器压制具身/外观特定信息，使视觉-语言条件更“任务本质化”，改善 OOD 与跨域泛化。
- 多模态人类示范：OmniShare 同时含运动学与触觉，提供高质量监督，提升数据效率并降低机器人遥操作采集成本。
- 端到端 VLA + 扩散式生成：以扩散 Transformer 建模动作分布，兼顾多模态输出与不确定性，适合灵巧操作的高自由度控制。
- 实证覆盖关键落地维度：零样本迁移、未见物体/环境、少样本适配都给出正面结果，具备实际应用潜力。

# 8. 不足与局限
- JAAS 的普适性边界：以“15 指关节 + 手腕 SE(3)”为规范，可能对结构差异更大的手部硬件或非手式末端执行器存在近似误差或表达不足。
- 域对抗的权衡：去具身化可能在极端情况下抑制部分与任务相关、但与域相关的有效线索，存在欠拟合风险，需要精细的权重与训练稳定性控制。
- 数据分布与偏差：尽管引入触觉与大规模人类示范，真实世界长尾物体/材质/环境的覆盖度与触觉分辨率是否足以支持更复杂操作尚不明确。
- 任务广度与可迁移性：提供内容未披露跨更多机器人平台、更多自由度手型、以及长时序多步骤任务的系统性评测。
- 资源开销：VLM + 域对抗 + 扩散 Transformer 的推理与训练可能较重，实用部署的实时性与能耗未见量化。
- 复现实验细节：算力、训练时长、超参与预训练细节未披露，影响可复现性与公平对比的可验证性。

（完）
