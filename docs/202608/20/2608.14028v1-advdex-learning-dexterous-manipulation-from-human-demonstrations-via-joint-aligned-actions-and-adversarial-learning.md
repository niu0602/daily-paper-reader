---
title: "AdvDex: Learning Dexterous Manipulation from Human Demonstrations via Joint-Aligned Actions and Adversarial Learning"
title_zh: AdvDex：通过关节对齐动作与对抗学习，从人类示范中学习灵巧操作
authors: "Zhiyue Zhao, Jingyi Wu, Hairuo Liu, Mingyu Liu, Liyang Li, Hengdi Zhang, Tong He, Zhengxue Cheng"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14028v1"
tags: ["query:dexop"]
score: 9.5
evidence: 从人类演示和多模态数据集中学习灵巧操作
tldr: AdvDex提出统一视觉-语言-动作框架，针对灵巧操作难以规模化与跨载体差异，构建含运动学与触觉的人类演示数据集OmniShare，提出关节对齐动作空间JAAS并用域对抗学习消除载体偏差；在手部动作预测与真实操作上优于基线，支持零样本人到机迁移与少样本自适应。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14028v1/fig-001.webp\", \"caption\": \"Figure 1: AdvDex. A unified Vision-Language-Action framework for scalable cross-embodiment dexterous manipulation. By aligning hardware kinematics through a shared action space and reducing embodiment-specific visual information through domain-adversarial learning, AdvDex supports zero-shot generalization to novel objects and unseen environments.\", \"page\": 2, \"index\": 1, \"width\": 822, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2608.14028v1/fig-002.webp\", \"caption\": \"Table 3: Success rates on real-world robot dexterous manipulation tasks (in %).\", \"page\": 7, \"index\": 2, \"width\": 818, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2608.14028v1/fig-003.webp\", \"caption\": \"Figure 4: Overview of the Domain-Adversarial VLA. A domain discriminator connected to the Vision-Language Model through a Gradient Reversal Layer encourages the extracted cognition tokens to become more domain-invariant before they condition the Diffusion Transformer for joint-aligned action prediction.\", \"page\": 5, \"index\": 3, \"width\": 814, \"height\": 280}]"
motivation: 机器人演示昂贵且不同载体动作空间不一致，异构数据易将任务视觉线索与外形绑定，阻碍跨载体泛化。
method: 构建OmniShare多模态人类演示数据集，设计包含SE(3)腕位姿与15指关节的JAAS统一动作表示，并以域对抗学习去除载体特征。
result: 在手部动作预测与真实灵巧操作任务上持续优于基线，实现零样本人到机技能迁移、对新物体与环境的泛化及高效少样本适配。
conclusion: 统一的关节对齐动作表示结合域对抗学习与高质量人类示范，可有效缩小人机差异并提升灵巧操作的可扩展性。
---

## 摘要
灵巧操作是具身智能的一项基础能力，但其规模化仍然困难，原因在于机器人示范的采集成本高昂，且不同具身形态的行为空间各不相同。在异质数据上训练的策略还可能将与任务相关的视觉线索与具身形态特定的外观纠缠在一起，从而限制跨具身形态的泛化。我们提出 AdvDex，一个统一的视觉-语言-动作框架，用于从人类与机器人示范中学习灵巧操作。首先，我们引入 OmniShare，一个大规模多模态的人类操作示范数据集，提供高质量的运动学监督和触觉测量，同时降低对机器人遥操作的依赖。其次，我们提出关节对齐动作空间（JAAS），这是一种规范化的动作表示，由一个$\mathrm{SE}(3)$手腕位姿和15个手指关节组成，从而在功能上对齐人手、灵巧机器人手与平行夹爪。最后，我们采用域对抗学习来减少学习到的视觉表征中的具身形态特定信息。在手部动作预测和真实世界灵巧操作上的实验表明，相较于基线方法取得了一致的提升，实现了有效的零样本人到机器人的技能迁移，对未见过的物体与环境具有良好的泛化能力，并能以较少数据实现高效的少样本自适应。

## Abstract
Dexterous manipulation is a fundamental capability for embodied intelligence, but scaling it remains difficult because robot demonstrations are expensive to collect and action spaces vary across embodiments. Policies trained on heterogeneous data can also entangle task-relevant visual cues with embodiment-specific appearance, limiting cross-embodiment generalization. We present AdvDex, a unified Vision-Language-Action framework for learning dexterous manipulation from human and robot demonstrations. First, we introduce OmniShare, a large-scale multimodal dataset of human manipulation demonstrations that provides high-quality kinematic supervision and tactile measurements while reducing reliance on robot teleoperation. Second, we propose the Joint-Aligned Action Space (JAAS), a canonical action representation comprising an $\mathrm{SE}(3)$ wrist pose and 15 finger joints, thereby functionally aligning human hands, dexterous robot hands, and parallel grippers. Finally, we use domain-adversarial learning to reduce embodiment-specific information in the learned visual representation. Experiments on hand-action prediction and real-world dexterous manipulation show consistent improvements over baselines, effective zero-shot human-to-robot skill transfer, generalization to unseen objects and environments, and data-efficient few-shot adaptation.

---

## 论文详细总结（自动生成）

# AdvDex：论文结构化总结

以下总结基于用户提供的摘要、图注与元数据信息整理，未覆盖论文全部细节。

## 1. 核心问题与整体含义（研究动机与背景）
- 问题背景
  - 灵巧操作（dexterous manipulation）是具身智能的核心能力，但难以规模化学习：
    - 机器人示范采集昂贵、效率低。
    - 不同具身形态（人手、灵巧机器人手、平行夹爪）在运动学与动作空间上不一致，导致跨载体迁移困难。
  - 在异构数据上训练的策略容易将“任务相关视觉线索”与“具身特定外观/域信息”纠缠，削弱跨载体与跨场景泛化。
- 论文目标
  - 提出一个统一的视觉-语言-动作（VLA）学习框架，能利用人类与机器人示范共同训练，缩小人机差异，并提升跨载体、跨场景泛化与零样本迁移能力。
- 整体含义
  - 通过统一动作表示（关节对齐动作空间 JAAS）与域对抗学习，结合高质量多模态人类演示数据集（OmniShare），构建可扩展的跨具身形态灵巧操作学习方案，覆盖从数据、表示到训练范式的闭环。

## 2. 方法论：核心思想、关键技术细节与流程
- 核心思想
  - 用一个功能对齐的统一动作表示（JAAS）跨越不同具身形态的运动学差异；
  - 用域对抗学习在视觉编码阶段抑制具身域信息，使视觉表征更“任务本质”而非“外观绑定”；
  - 用大规模人类演示（含精确运动学与触觉）代替昂贵的机器人遥操作，大幅扩展数据来源。

- 关键组件
  1) 关节对齐动作空间 JAAS
     - 表示定义：
       - 手腕位姿：SE(3)（3D平移 + 3D旋转）。
       - 手指关节：15个自由度（覆盖灵巧手与人手主要动作维度）。
     - 功能：
       - 对齐人手、灵巧机器人手与平行夹爪的动作语义，用“可执行的功能空间”而非“原生关节空间”建立统一接口；
       - 便于从人类演示到不同机器人手型的“动作重定向/映射”（retargeting）。
     - 直观流程（文字化）：
       - 采集人手或机器人手的动作轨迹 → 标准化到 JAAS 表示 → 针对目标载体的运动学映射/逆运动学求解 → 输出目标载体的执行命令。

  2) 域对抗的 VLA（Vision-Language-Action）策略网络
     - 架构要点（见图注“Figure 4”）：
       - 视觉-语言编码器（VLM）提取“认知 token”（cognition tokens）；
       - 通过梯度反转层（GRL）连接一个域判别器，判别器试图区分具身域（如人 vs 机器人），GRL让主干编码器学到“域不变”的视觉表征；
       - 下游由扩散式 Transformer（Diffusion Transformer）在认知 token 条件下预测 JAAS 动作分布。
     - 训练目标（文字化）：
       - 主任务：最大化 JAAS 动作的似然/最小化重构或扩散反向过程损失；
       - 域对抗：最小化域判别器的准确性（通过 GRL 反向逼迫表征域不变），从而降低具身外观信息对策略的干扰。

  3) OmniShare 数据集
     - 多模态的大规模人类操作示范：
       - 含高质量运动学监督（精确手部姿态/关节）与触觉测量；
       - 减少对机器人遥操作数据的依赖；
       - 为零样本人到机迁移提供丰富先验。
     - 用途：
       - 训练 VLA 策略（与机器人示范联合）；
       - 提升手部动作预测与策略泛化。

- 端到端训练与推理流程（文字版）
  1) 输入：场景视觉（可能含多视角）、任务语言描述（如目标动作/指令），可选地结合机器人状态；
  2) 视觉-语言编码器抽取认知 token；
  3) GRL + 域判别器：对抗式约束，使认知 token 域不变；
  4) 扩散 Transformer：条件于认知 token，生成 JAAS 动作序列（手腕 SE(3) + 15 关节）；
  5) 动作重定向：将 JAAS 映射到目标载体（人手、灵巧手、平行夹爪）对应命令；
  6) 执行与闭环评估：在真实机器人或仿真中执行，评估任务成功率。

## 3. 实验设计：数据/场景、基准与对比
- 任务与场景
  - 手部动作预测（hand-action prediction）：用视觉/语言输入预测 JAAS 表示；
  - 真实世界灵巧操作：在真实机器人平台上执行多项精细操作任务；
  - 泛化设置：零样本人到机迁移、对未见物体与新环境的泛化、少样本自适应。
- 数据来源
  - OmniShare（人类演示，多模态：运动学 + 触觉）；
  - 机器人示范（数量未披露，作为联合训练或适配用）。
- 评测基准与指标
  - 手部动作预测：预测误差或准确性（具体指标未在提供内容中披露）；
  - 真实机器人：任务成功率（见“Table 3: Success rates ...”，数值未在本摘要中给出）。
- 对比方法
  - 与“基线方法”进行比较（具体模型名称与实现未在提供内容中说明）；
  - 从图注与摘要可见还包含“域对抗模块”的对比/消融线索，但详细配置未披露。

## 4. 资源与算力
- 论文所用算力（GPU 型号、数量、训练时长等）在当前提供内容中未明确说明。

## 5. 实验数量与充分性
- 实验覆盖面（从已提供内容判断）：
  - 至少包含两大类实验：手部动作预测、真实机器人执行；
  - 泛化设置包括：零样本人到机、未见物体/环境、少样本自适应；
  - 存在表格呈现的多任务成功率（表 3），显示在不同任务上与基线比较。
- 充分性与公平性（基于有限信息的判断）：
  - 任务类型多样，包含真实机器人验证，具有说服力；
  - 但未见到具体任务清单、对象数量、训练/测试划分、随机种子与统计显著性等细节；
  - 基线选择、超参一致性与复现实验细节在当前内容中缺失，无法充分评估公平性与客观性。

## 6. 主要结论与发现
- 使用 JAAS 与域对抗 VLA 的 AdvDex，在以下方面优于基线：
  - 手部动作预测精度更高；
  - 真实世界灵巧操作成功率更高（表 3 展示了百分比对比，但具体数值未提供）；
- 能力与泛化：
  - 零样本人到机器人技能迁移有效；
  - 对未见物体与新环境具有良好泛化；
  - 少样本自适应高效，数据效率更好。

## 7. 优点（亮点）
- 表示层面的统一：JAAS 用“手腕 SE(3) + 15 关节”构造跨载体可执行的统一动作空间，显著缓解人机运动学差异。
- 表征层的域不变性：通过 GRL 的域对抗学习减少具身外观/域信息对策略的干扰，提升跨载体与跨场景泛化。
- 数据侧的可扩展性：OmniShare 提供高质量人类演示（含触觉、运动学），降低遥操作依赖，利于规模化。
- 模型架构的可组合性：视觉-语言条件下的扩散 Transformer 预测动作，适配多任务与多模态输入。
- 实证覆盖真实机器人：不仅做预测，还在真实平台上验证成功率与迁移能力。

## 8. 不足与局限
- 实验与细节透明度（基于目前信息）：
  - 缺乏基线名称、具体任务与对象数量、指标定义、统计显著性报告等细节，难以全面评估公平性与泛化强度；
  - 训练算力与数据规模未披露，不利于复现与工程评估。
- 动作表示的潜在局限：
  - JAAS 采用 15 个手指关节与手腕 SE(3)，对更多自由度或特殊结构的手型可能仍需额外映射与调参；
  - 对夹爪等低自由度手型的“功能对齐”可能对某些复杂内手操作仍有表达瓶颈。
- 视觉-域对抗的风险：
  - 域对抗可能抑制一部分潜在有用的域特征，若任务依赖某些具身特征（如手指外形与接触几何），可能损失性能；
  - 域判别器的选择与训练稳定性对最终表征影响较大（未见到更详细的稳定性讨论）。
- 触觉的使用方式：
  - 虽然数据集中包含触觉测量，但当前信息未明确其在策略训练中的融合策略与收益拆解；触觉与视觉、语言的融合与对齐仍可能是后续优化点。

（完）
